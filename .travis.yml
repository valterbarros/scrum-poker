language: ruby
rvm: 
  - 2.4.2
env:
  - DB =pgsql
services:
  - postgresql
before_script:
  - RAILS_ENV=test bundle exec rake db:create && rake db:migrate
script:
  - cp config/database.yml
  - bundle exec rspec spec
