<% @session_votes.each do |session_vote|  %>
  <hr />
  <p>Created_at: <%= session_vote.created_at  %></p>
  <p>Title: <%= session_vote.title  %></p>
  <%= link_to "Enter", mount_show_path(session_vote)  %>
  <hr />
<% end %>
